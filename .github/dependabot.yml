version: 2
updates:
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "saturday"
      time: "01:00"
    open-pull-requests-limit: 3
    groups:
      patch-updates:
        patterns: [ "*" ]
        update-types: [ "patch" ]
    ignore:
      - dependency-name: "org.jetbrains.kotlin*"
        versions: ["2.1.x"] # Ignore minor updates for Kotlin
      - dependency-name: "io.mockk:mockk"
        update-types:
          - "version-update:semver-patch" # Ignore patch updates until 1.15.0
    commit-message:
      prefix: "build"
      include: "scope"
